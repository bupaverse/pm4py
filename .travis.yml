# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

dist: trusty
r: release
latex: false

cache:
  packages: true
  directories:
    - $HOME/.cache/pip

before_install:
  - sudo apt-get update
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
  - bash miniconda.sh -b -p $HOME/miniconda
  - rm miniconda.sh
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a
  - conda create -q -n r-reticulate python=$TRAVIS_PYTHON_VERSION pip

before_Script:
  - source activate r-reticulate
  - ls
  - set -a
  - source R/version.R
  - set +a
  - pip install $PM4PY_VERSION

# TODO somehow Travis / Reticulate does not use pip even if forced
#  - R -e 'pm4py::install_pm4py(method = "conda")'
