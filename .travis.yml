# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

dist: trusty
sudo: false

r: release
latex: false

cache:
  packages: true
  directories:
    - $HOME/.cache/pip

env:
  global:
   - RETICULATE_PYTHON="/usr/bin/python3"

before_script:
  - sudo apt-get update
  - sudo apt-get install python3-pip
  - pip3 install --upgrade --ignore-installed --user travis virtualenv
  - pip3 install --upgrade --ignore-installed --user travis numpy
  - R CMD INSTALL .
  - R -e 'pm4py::install_pm4py()'
